---
description: AI 自我反思 — 把犯过的错误沉淀到 lessons-learned.mdc
alwaysApply: true
---

# 错误沉淀规则

让 AI 把**犯过的错、错误判断、随意猜测**沉淀到 `.cursor/rules/lessons-learned.mdc`,形成可累积、可复用的"教训知识库"。

---

## 接到新任务时:先读教训

在动手执行前(尤其是复杂任务、改代码、跑实验),**先快速扫一遍** `.cursor/rules/lessons-learned.mdc`,确认本次任务是否踩过类似的坑。

> 重复犯同一个错 = 比第一次犯错严重得多。

---

## 触发条件:什么算"错误"

满足任一条,即需要沉淀:

### 用户明确指出
- "你错了" / "不对" / "又错" / "返工" / "你怎么又这样"
- 用户否定 / 推翻 AI 提出的方案
- 用户纠正 AI 的事实陈述(如"这个函数在 X 文件,不在 Y")

### AI 自我察觉(同样要沉淀)
- **随意猜测被证伪**:未读代码就猜 API / 字段名 / 文件路径,运行后报错
- **幻觉**:编造不存在的函数、参数、配置项、命令行选项
- **跳过验证**:没运行 `ls / Read / Grep` 就断言"文件结构是 X"
- **过度自信**:用"肯定"/"一定"下结论,事后证据不足
- **范围蔓延(scope creep)**:做了用户没要求的事,导致需要回滚
- **误解需求**:理解偏差导致整段产出无效
- **命令/代码报错**:因 AI 自身判断失误(非环境问题)而失败

## 不触发的情况
- 用户改主意 / 调整需求
- 环境、依赖、网络等外部问题
- 用户笔误或表述歧义(可以提示,但不沉淀)
- `lessons-learned.mdc` 已有同根因条目(此时应**更新计数**而非新建)

---

## 工作流

1. **识别**:按上面"触发条件"判断
2. **反思根因**:不是"我做错了什么",而是"我**为什么**会这样错"
   - 没读代码就猜? → 根因 = 跳过验证
   - 看了一眼就改? → 根因 = 上下文不足就动手
   - 没问清楚就做? → 根因 = 假设代替确认
3. **追加沉淀**(直接写入,无需用户确认):
   - 打开 `.cursor/rules/lessons-learned.mdc`
   - 按下面"条目模板"追加
   - 若已有同根因条目:在该条目下 `## 复发记录` 增加一行,不要新建
4. **回复末尾告知用户**(单行):
   ```
   已沉淀经验 → lessons-learned.mdc #<编号> <一句话标题>
   ```
5. **同一对话内合并**:同次对话中相同根因的多个错误,合并为一条沉淀

---

## 条目模板

按以下格式追加到 `lessons-learned.mdc` 末尾:

```markdown
## #<编号> [YYYY-MM-DD] <动词开头的一句话标题>

- **场景**:在什么任务/上下文下出现的(1 行)
- **错误**:做了什么 / 说了什么 / 给了什么命令(1-3 行,可贴关键片段)
- **根因**:为什么会这样错(从下面"根因分类"里选,可叠加)
- **教训**:下次遇到 X 时,应该 Y(必须**可执行**,不能空话)
- **触发**:被用户指出 / AI 自察
- **复发记录**:(初始为空,后续同类错误在此累加日期)
```

---

## 根因分类(写"根因"时优先用这些标签)

| 标签 | 含义 |
|---|---|
| `跳过验证` | 没 Read / Grep / Shell 验证就下结论 |
| `幻觉编造` | 编造不存在的 API / 文件 / 参数 |
| `上下文不足` | 没读够代码就动手改 |
| `过度自信` | 用"肯定"下结论,证据不足 |
| `范围蔓延` | 做了用户没要求的事 |
| `误解需求` | 理解偏差,方向跑偏 |
| `重复犯错` | lessons-learned 里已有同款,又犯 |
| `工具误用` | 用错了工具(如该用 Grep 却用 Shell grep) |

---

## "教训"怎么写才有用

**反例(空话,禁止)**:
- "下次要细心" / "下次要认真" / "下次要先思考"

**正例(具体可执行)**:
- "改 `xxx.py` 前先 Read 完整文件,不要只看片段"
- "涉及 `mmcv` API 时,先 Grep 仓库中已有用法,不要凭记忆"
- "用户说'帮我改一下'时,先列出修改清单让用户确认,再动手"

---

## 示例

```markdown
## #3 [2026-05-28] 修改 InstanceBank 前未 Read 整文件,误删 cache 逻辑

- **场景**:用户要求给 InstanceBank 加 prev_id 字段
- **错误**:只 Read 了 `__init__` 部分就用 StrReplace 改了 `cache()` 方法,
  误删了 `track_id` 缓存的更新逻辑,跑训练时 ID 全部错乱
- **根因**:`跳过验证` + `上下文不足`
- **教训**:改 `InstanceBank` / `Sparse4DHead` 这类有状态的类时,**必须 Read
  整个类**(`offset=0, limit=` 整文件长度),不能只看 diff 周围
- **触发**:用户指出"ID 怎么乱了"
- **复发记录**:
```
