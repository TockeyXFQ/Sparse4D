---
description: AI 累积的教训知识库 — 接到新任务时务必先扫一遍
alwaysApply: true
---

# 教训知识库 (Lessons Learned)

> 本文件由 AI 按 `self-reflection.mdc` 规则**自动追加**。
> 接到新任务前,先扫一遍下方条目,避免重复踩坑。

---

## 维护规则(给 AI 看)

1. **新建条目**:按 `self-reflection.mdc` 的"条目模板"追加到本文件**末尾**
2. **编号**:递增整数(#1, #2, #3 ...),不复用、不跳号
3. **同根因复发**:不新建条目,在原条目 `## 复发记录` 下加一行 `- YYYY-MM-DD: <场景>`
4. **不删除条目**:即使过期,也保留(可标 `~~已修复~~` 删除线),保留可追溯性
5. **每条目独立成节**:用 `## #N` 起始,方便 Grep `^## #` 统计总数

---

## 索引(可选维护)

> 当条目超过 20 条时,在此按"根因标签"做快速索引。当前条目数较少,留空。

---

## 已记录的教训

<!-- 新条目追加到此分隔线下方 -->

## #1 [2026-05-28] 用 sample 0 验证 pkl 字段,误判"全 0"导致用户多决策一轮

- **场景**:Phase 2 启动前,验证 nuScenes pkl 的 `sweeps` 字段是否填充
- **错误**:只 Read pkl 的 `infos[0]`(`sample 0`)就断言"`sweeps len: 0`,
  pkl 不含 LiDAR sweeps,**需要重跑 converter ~6-8h**",让用户花时间考虑
  "是否需要 6-8h 数据准备 + LiDAR sweep 决策"。后来重新检查 sample 1 / 2
  / 100 / 500 才发现:**只有 sample 0(每个 scene 的起点)没有 prev sweep,
  其他 sample 都有 sweeps len=10**,pkl 完全 OK,根本不用重跑 converter。
- **根因**:`跳过验证` + `过度自信`(只查 1 个特殊样本就外推结论)
- **教训**:验证 pkl / dataset / 任何 list-of-dict 字段含量时,**必须采样
  多个 index**(至少 `[0, 1, len/2, -1]` 4 个),特别注意"边界 index"
  (如 sample 0 是 scene 起点,这种特殊样本不能代表整体)。**不要因为
  单个样本字段为空就泛化结论**。给用户做"重大决策建议"前(尤其是涉及
  数小时工作量的),必须采样 ≥3 个 index 验证。
- **触发**:AI 自察(用户后续质疑后重新验证才发现)
- **复发记录**:

## #2 [2026-05-28] 用 mmdet build_backbone 构造 mmdet3d 的 SECOND,registry KeyError

- **场景**:Phase 2 写 Sparse4D 主类加 LiDAR backbone(SECOND/SECONDFPN
  /HardSimpleVFE/SparseEncoder)
- **错误**:在 `sparse4d.py` 里直接用 `from mmdet.models import build_backbone`
  来构造 `pts_backbone=dict(type='SECOND')`,运行时报
  `KeyError: 'SECOND is not in the models registry'`。原因是 SECOND/
  SECONDFPN/HardSimpleVFE/SparseEncoder 注册在 **mmdet3d 自己的 registry**,
  不在 mmdet。同样 mmcv 的 `_base_` 继承不允许 `None → dict` 的字段类型变化
  (子 config 加 `masked_modal_prob=dict(...)`,base 写 `masked_modal_prob=None`
  会触发 `TypeError: ... cannot inherit from base because ... is a dict in
  the child config but is of type NoneType in base config`)。
- **根因**:`幻觉编造`(凭直觉用 build_backbone)+ `跳过验证`(没去 Grep
  mmdet3d 库的 builder.py)
- **教训**:
  1. 涉及 **LiDAR backbone**(SECOND/SECONDFPN/HardSimpleVFE/SparseEncoder
     /PointPillarsScatter 等)必须用
     `from mmdet3d.models.builder import build_backbone, build_neck,
     build_voxel_encoder, build_middle_encoder`,**不是 mmdet 的 builder**。
  2. 写 mmcv `_base_` 继承的 config 时,**可选字段在 base 不要显式写
     `field=None`**,直接省略让父 config 不指定该字段;或者 child 用
     `_delete_=True` 强制覆盖。这条规则适用于所有 `Optional[dict]` 类型
     的字段(如 `masked_modal_prob`)。
- **触发**:AI 自察(开发时 build_detector 报错)
- **复发记录**:

## #3 [2026-05-28] 写 multi-node auto-scaling 只 scale lr,漏 scale iter 数,差点让用户多机训练过训 20×

- **场景**:写 `scripts/05_train_multinode.sh` 的"多机自动适配"功能,
  根据 `TOTAL_GPUS` 自动调整训练超参,用户启动命令一行不变。
- **错误**:**只想到 `optimizer.lr` 一个字段需要 scale**,提交了脚本 + md 文档
  说"用户只需 set CONFIG,其他全自动"。直到用户问"`expF_full.py` 的 epoch
  怎么看",我审查 baseline 主 config 才发现 `num_iters_per_epoch=586` 是用
  `num_gpus=8` hardcode 算的:
  ```python
  num_iters_per_epoch = int(28130 // (num_gpus * batch_size))  # = 586
  runner.max_iters = num_iters_per_epoch * num_epochs  # = 58600 (=100 epoch)
  ```
  **多机时 mmcv 不会自动重算 num_iters_per_epoch**,如果不在脚本里 scale
  `runner.max_iters / evaluation.interval / checkpoint_config.interval`,
  20 机 160 卡跑出来会是 **2000 epoch(过训 20×)**,数据训出来全废,
  P2 多机训练全部要重做。
  
  **代价测算**:如果用户没问 epoch 这个问题,直接拿脚本去 20 机 160 卡跑 P2,
  18-24h 后会发现训练没收敛(过训严重),整个训练废掉,损失 1 天 GPU 时间 +
  调试时间。
- **根因**:
  1. **没系统列清单**:写"auto-scaling"功能时,凭直觉只想到 lr,没系统地问
     "batch size 放大 N×,baseline config 里**所有**跟 batch / num_gpus /
     iter 数有关的字段都有哪些?"
  2. **定式思维**:"lr scaling" 是大 batch training 的经典话题,我习惯性
     只关注 lr,完全忽略 iter 数也是 batch-dependent 字段。
  3. **审查 config 不完整**:写脚本时只 grep 了 `optimizer.lr`,没系统看
     baseline config 末尾的 runner / evaluation / checkpoint_config 三块。
- **教训**(可执行):
  1. **写"auto-scaling"功能前,先列 batch-dependent 字段清单**。mmcv config
     里跟 batch size / num_gpus / iter 数有关的所有字段:
     ```
     [必 scale - 否则过训/欠训]
     - runner.max_iters                 (1/N scale,保持总 epoch 不变)
     - evaluation.interval              (1/N scale,保持评测频次)
     - checkpoint_config.interval       (1/N scale,保持 ckpt 频次)
     [推荐 scale - 不爆梯度]
     - optimizer.lr                     (sqrt(N) scale 推荐)
     [视情况]
     - lr_config.warmup_iters           (large batch 不需要更长 warmup,
                                         我们的 baseline 500 iter 在 160 卡
                                         下占比 17% 仍可接受,默认不动)
     [不要 scale]
     - data.samples_per_gpu             (单卡 batch,跟 num_gpus 解耦)
     - lr_config.min_lr_ratio           (相对 lr,不用 scale)
     - optimizer.weight_decay           (跟 batch 无关)
     ```
  2. **写代码前 review 这个清单**,逐个判断"这个字段在多机时该不该变"。
     如果一个该 scale 的字段忘了 scale,后果可能是数据训废 + 整批实验重做。
  3. **遇到 hardcode 的 batch-dependent 计算公式时(如 `num_gpus=8`),立刻
     标记**这是潜在 bug 源,因为多机时不会自动 propagate。可以加个 inline
     comment 提醒未来读者:`# WARNING: num_iters_per_epoch 用 num_gpus=8
     hardcode,多机训练用 05_train_multinode.sh 自动 scale max_iters`。
  4. **凡是写"用户只需做 X,其他全自动"这种宣称,必须列清单证明"全"覆盖了**。
     提交前 review:
        - 多机调度时改了什么 → batch_size 翻 N 倍
        - 哪些字段跟 batch_size 有关? → 列清单
        - 我都 scale 了吗? → 逐个核对
- **触发**:用户指出"为什么会犯这么低级的错误"(自我审查 baseline config
  时偶然发现,如果用户没问 epoch 这个问题,bug 会延迟到训练完成才暴露)
- **复发记录**:

## #4 [2026-05-29] smoke test 只看"跑完 + 存 ckpt"就宣称通过,没看 loss 是否 NaN

- **场景**:P2 F1 (LiDAR sum fusion) 写完,跑 1 卡 50-iter smoke test 验证
- **错误**:smoke 跑完看到 `Saving checkpoint at 50 iterations` + `Reducer
  buckets rebuilt` + NCCL 正常退出,就宣称 "smoke 通过,可以上多机训练"。
  **完全没检查 loss 值**。实际上:
  1. smoke 设 `SMOKE_ITERS=50`,但 log_config.interval=51 → **一行 loss 都没
     被记录**(第一个 log 点在 iter 51,而 smoke 只到 50)。我连一个 loss
     数字都没看到,就说"通过"。
  2. 用户拿去 2 机 16 卡跑真训练,**从 iter 51 第一个 log 点开始就是 loss=nan**,
     训了 3000+ iter 全是 NaN,白训。
- **根因**:`过度自信` + `跳过验证`(把"进程没崩溃 + 存了 ckpt"误当成
  "训练健康")
- **教训**(可执行):
  1. **smoke test 的通过标准必须包含"loss 是有限值且在合理量级且呈下降
     趋势"**,不能只看"进程跑完 / 存了 ckpt"。"能跑完" ≠ "训得对"。
  2. **smoke 的 iter 数必须 > log_config.interval**(否则一行 loss 都看不到)。
     Sparse4D log interval≈51,smoke 至少跑 `SMOKE_ITERS=110`(保证 ≥2 个
     log 点能看趋势),或者临时 `--cfg-options log_config.interval=10`。
  3. **新模块(尤其改了 forward / loss / 混合精度的)smoke 完,必须用脚本
     解析 .log.json 确认 `loss` 字段 `not isnan`**,再宣称通过。可以写成
     smoke 脚本末尾的自动断言:跑完 grep log.json,任何一行 loss=nan 就报错。
- **触发**:用户拿 smoke "通过" 的代码去真训练,发现 loss 全程 NaN
- **复发记录**:

## #5 [2026-05-30] NaN 连续 3 次"修好"被证伪 — 靠推断改而非靠实测定位

- **场景**:P2 LiDAR 融合训练反复 NaN,我连续给了 3 个"修复"
- **错误**:
  1. 第 1 次:NaN from iter 51 → 我推断"fp16 spconv 权重"→ 改 fp32。
     实测证伪(fp32 后 NaN 推迟到 iter 5865,没根治)。
  2. 第 2 次:iter 5865 渐进梯度爆炸(grad→2480)→ 推断"未归一化残差
     分支"→ 加 LayerNorm+gate。**这次方向对**(渐进爆炸确实修好,grad
     稳定),但我用 150-iter smoke 就说"验证通过",而崩盘点在 iter 5865,
     smoke 范围内根本测不到 → 又是 #4 同类(验证范围不覆盖故障点)。
  3. 第 3 次:有 LN 后 iter 3111 仍 NaN(但这次 grad 健康时突变,是
     data-dependent 单点 NaN,跟前两次根因不同)。
  共性:**我一直在"看现象→推断根因→改代码"循环,而没有第一时间加
  instrumentation 去实测 NaN 到底从哪个 tensor 起源**。
- **根因**:`过度自信`(把"机制上合理的修复"当成"已验证的修复")+
  `跳过验证`(没在第一时间加 NaN-source 定位 hook,靠脑补分支)
- **教训**(可执行):
  1. **遇到 NaN/Inf,第一步不是猜根因,而是加 instrumentation 定位源头**:
     - forward 各阶段加 `torch.isnan(x).any()` 检查,打印第一个变 NaN 的
       tensor 名(image feat? lidar feat? 某 loss 项?)
     - 或直接用 `torch.autograd.set_detect_anomaly(True)` 抓 backward NaN
     - 定位到具体 tensor 后再谈修复,**禁止"现象→推断→改"跳过定位**
  2. **训练类 NaN 修复的"验证"必须覆盖原故障点**:故障在 iter N,验证就
     必须跑过 iter N(或在能复现的最小设置下复现 NaN 再确认消失)。
     150-iter smoke 验证不了 iter 5865 的崩盘 —— 这种"验证"等于没验证。
  3. **NaN 有多种独立根因,修好一种不等于修好全部**:渐进梯度爆炸(优化
     不稳定,grad 飙升)vs 突发单点 NaN(脏数据/除零,grad 健康时突变)是
     两类不同的病,要分别用不同手段(前者 norm/gate/降 lr,后者 nan_to_num
     净化 + 定位脏数据)。看 grad 轨迹区分:渐进飙升 vs 健康突变。
  4. **已知数值放大器(实测)**:`LayerNorm(含 inf 的输入) → 输出整向量
     全 nan`(inf 参与 mean/var → inf-inf=nan 扩散)。任何 LN/BN 前的、
     可能含 inf 的张量,先 `torch.nan_to_num` 净化。
  5. **预防性工程**:训练脚本/config 默认挂 NaN 早停 hook(loss=nan 立即
     终止),避免在崩坏模型上白烧 GPU(本次又白跑 ~800 iter 才发现)。
     已实现 `NaNStopHook`(projects/mmdet3d_plugin/core/hook/)。
- **触发**:用户连续贴 3 次 NaN 训练 log;第 3 次后我才转向"加定位 hook"
  而非继续推断
- **复发记录**:
